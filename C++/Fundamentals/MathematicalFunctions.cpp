#include <algorithm>
#include <iostream>
#include <cmath>

int main (int argc, char *argv[]) {
    double number_exp = 1.0;
    std::cout << std::exp(number_exp) << std::endl;
    std::cout << std::floor(2.1) << std::endl;
    std::cout << std::min(2, 3) << std::endl;
    std::cout << std::max(2, 4) << std::endl;
    return 0;
}
